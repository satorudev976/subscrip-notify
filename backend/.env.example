# Google OAuth (iOS client — no client secret needed)
GOOGLE_CLIENT_ID=

# LINE Messaging API
LINE_CHANNEL_ACCESS_TOKEN=
LINE_CHANNEL_SECRET=

# Encryption key for refresh tokens (base64-encoded 32 bytes)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
TOKEN_ENCRYPTION_KEY=

# Cloud Scheduler shared secret (optional if using IAM auth)
SCHEDULER_SECRET=

# Server port (Cloud Run sets this automatically)
PORT=8080
